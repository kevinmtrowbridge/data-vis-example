<%= form_for(@workspace_dataset) do |f| %>
  <% if @workspace_dataset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workspace_dataset.errors.count, "error") %> prohibited this workspace_dataset from being saved:</h2>

      <ul>
      <% @workspace_dataset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dataset_id %><br>
    <%= f.number_field :dataset_id %>
  </div>
  <div class="field">
    <%= f.label :workspace_id %><br>
    <%= f.number_field :workspace_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
