<%= form_for(@dataset) do |f| %>
  <% if @dataset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dataset.errors.count, "error") %> prohibited this dataset from being saved:</h2>

      <ul>
      <% @dataset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :data_source_id %><br>
    <%= f.number_field :data_source_id %>
  </div>
  <div class="field">
    <%= f.label :hdfs_data_source_id %><br>
    <%= f.number_field :hdfs_data_source_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
